# 필수
ANTHROPIC_API_KEY=sk-...        # Python AI 자가치유 LLM (현재 기준 anthropic api 사용)

# 선택 (기본값으로 동작)
DB_PASSWORD=doma                # PostgreSQL 비밀번호
DOMA_API_TOKEN=                 # 외부 데이터 API(/api/v1/**) 인증 토큰
