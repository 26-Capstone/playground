// 셀렉터 피커(server.js)와 실제 스크래퍼(scraper.js)가 서로 다른 뷰포트로 페이지를 열면,
// 반응형 사이트(예: 토스증권)는 완전히 다른 DOM 구조를 렌더링할 수 있다 — 피커에서 고른
// 셀렉터가 실제 스크래퍼 실행 시 "요소를 찾을 수 없음"으로 실패하는 원인이었다. 두 곳이
// 항상 같은 뷰포트를 쓰도록 여기서 한 곳만 관리한다.
const VIEWPORT = { width: 960, height: 600 };

module.exports = { VIEWPORT };
