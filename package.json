{
  "name": "mender",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "npm start --workspace=server",
    "dev":   "npm run dev --workspace=server"
  }
}
